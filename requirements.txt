# Core dependencies
# Microsoft Agent Framework runtime (workflows + agents).
# The ``agent-framework`` beta meta-package currently requires
# ``agent-framework-core[all]`` and pulls mutually incompatible optional
# adapters on Python 3.13.  The course A0-A5 runtime imports only the core
# workflow API, so pin the compatible core distribution instead.
agent-framework-core==1.0.0b251211

# Data modeling / validation
pydantic>=2.7.0

# Course workspace files (courses/<id>/*.yaml)
pyyaml>=6.0

# Local configuration (.env) for CLI runs
python-dotenv>=1.0.1

# CLI
typer>=0.12.5

# Optional: better console output (Typer uses it when present)
rich>=13.9.0

# Simple local UI (run viewer + quiz)
streamlit>=1.37.0


# Backend API
fastapi>=0.110.0
uvicorn>=0.29.0
websockets>=12.0
python-multipart>=0.0.9
